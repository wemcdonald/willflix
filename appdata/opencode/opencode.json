{
  "$schema": "https://opencode.ai/config.json",
  "permission": "allow",
  "mcp": {
    "playwright": {
      "type": "local",
      "command": ["bunx", "@playwright/mcp@latest", "--headless", "--executable-path", "/opt/google/chrome/chrome"],
      "enabled": true
    }
  }
}
