[Unit]
Description=VPN
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=true
WorkingDirectory=/willflix/docker
ExecStart=/usr/bin/docker compose up -d vpn
ExecStop=/usr/bin/docker compose down vpn
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target
