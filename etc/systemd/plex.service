[Unit]
Description=Plex
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=true
WorkingDirectory=/docker/config
ExecStart=/usr/bin/docker compose up -d plex
ExecStop=/usr/bin/docker compose down plex
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target
