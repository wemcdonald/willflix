[Unit]
Description=Homarr
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=true
WorkingDirectory=/willflix/docker
ExecStart=/usr/bin/docker compose up -d homarr
ExecStop=/usr/bin/docker compose stop homarr
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target