#!/bin/bash

HOSTS="willflix.org admin audiobooks bookbag books calibre cam home proxmox request route mail"
for domain in $HOSTS; do
  [[ $domain = *.* ]] || domain="$domain.willflix.org"
  certbot certonly --webroot -w /var/www/html/letsencrypt --force-renewal -d $domain
done
