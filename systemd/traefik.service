[Unit]
Description=Traefik
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=true
WorkingDirectory=/docker/config
ExecStart=/usr/bin/docker compose up -d traefik
ExecStop=/usr/bin/docker compose down traefik
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target
